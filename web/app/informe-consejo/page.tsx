import PaginaEnConstruccion from "@/components/layout/PaginaEnConstruccion";

export default function PaginaInformeConsejo() {
  return (
    <PaginaEnConstruccion
      titulo="Informe de consejo"
      descripcion="Resumen ejecutivo de solo lectura del estado del portfolio."
    />
  );
}
